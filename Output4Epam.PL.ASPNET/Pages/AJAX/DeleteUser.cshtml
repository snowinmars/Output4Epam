@using Output4Epam.Providers
@using Output4Epam.Entities

@{
	if (IsPost)
	{
		string login = Request["Login"];

		LogicProvider.RegUserLogic.RemoveByLogin(login);
	}

	Response.Redirect("/Pages/StructureElements/AllUsers.cshtml");
	return;
}
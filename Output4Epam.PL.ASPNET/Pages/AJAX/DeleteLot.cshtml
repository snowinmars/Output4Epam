@using Output4Epam.Providers

@{
	if (IsPost)
	{
		Guid id = Guid.Parse(Request["Id"]);

		LogicProvider.LotLogic.Remove(id);
		Response.Redirect("/Pages/StructureElements/MyLots.cshtml");
		return;
	}
}